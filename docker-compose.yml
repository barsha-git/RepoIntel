version: "8.8.0"

services:

  redis:
    image: redis/redis-stack:latest
    container_name: RepoIntel-redis

    ports:
       - "6379:6379"
       - "8001:8001"

    restart: unless-stopped

    volumes:
      - redis_data:/data

volumes:
  redis_data: